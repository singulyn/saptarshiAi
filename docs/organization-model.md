# Organization Model

SaptariX uses Organization naming for customer, company, workspace, and account boundaries. Core tables use the `org` schema:

- `org.Organizations`
- `org.OrganizationUsers`
- `org.OrganizationApps`
- `org.OrganizationModules`
- `org.OrganizationSettings`

Do not use Tenant or Tenancy naming.
